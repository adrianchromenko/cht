{
  "name": "chtcht",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build:client": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.17.1",
    "http": "^0.0.1-security",
    "path": "^0.12.7",
    "socket.io": "^4.1.3",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
